version: 2.1
on: 
  push: 
  branches: main
jobs:
  build:
    docker:
      - image: python 
    steps:
      - run:
          name: Hello World
          command: |
            echo 'Hello World!'
            echo '$GIT_SHA1'
      
